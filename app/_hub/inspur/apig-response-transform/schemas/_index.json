{
  "fields": [
    {
      "consumer": {
        "eq": null,
        "type": "foreign",
        "reference": "consumers"
      }
    },
    {
      "protocols": {
        "elements": {
          "one_of": [
            "http",
            "https"
          ],
          "type": "string"
        },
        "required": true,
        "type": "set",
        "default": [
          "http",
          "https"
        ]
      }
    },
    {
      "config": {
        "fields": [
          {
            "format": {
              "type": "string",
              "required": true,
              "default": "xml",
              "examples": ["xml"],
              "description": "Describe the format of the response format."
            }
          }
        ],
        "type": "record",
        "required": true
      }
    }
  ]
}
